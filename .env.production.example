# Docker Compose automatically reads this file when it is copied to .env.
HOST_PORT=8787
ADMIN_PASSWORD=replace-with-a-long-unique-password

# Optional: external media storage (leave blank to use the persistent Docker volume).
TENCENT_COS_SECRET_ID=
TENCENT_COS_SECRET_KEY=
TENCENT_COS_BUCKET=
TENCENT_COS_REGION=ap-guangzhou
TENCENT_COS_CDN_URL=
